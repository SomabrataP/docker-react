sudo: required
services:
  - docker

before_install:
  - docker build -t smbrtprmnk/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true smbrtprmnk/docker-react npm run test -- --coverage
